def sum_concat(first,second)

		if first.is_a?(Integer) && second.is_a?(Integer)
			result = first+second
		elsif first.is_a?(String) && second.is_a?(String)
			result = first.concat(second)
		else
			raise TypeError,"you must enter number or string"
		end			
   
 end


 	sum_concat("adnan","adnan")
 	sum_concat(12,24)

output: adnanadnan
	36
	you must enter number or string
 	sum_concat(12,"adnan")

	